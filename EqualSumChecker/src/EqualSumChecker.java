public class EqualSumChecker {
    // write code here
    public static boolean hasEqualSum (int num1, int num2, int num3) {
        boolean b = num1 + num2 == num3 ? true : false;

        return b;
    }
}